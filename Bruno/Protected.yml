info:
  name: Protected
  type: http
  seq: 5

http:
  method: GET
  url: "{{backend-base-url}}{{api-stage}}/protected2"
  headers:
    - name: Authorization
      value: Bearer {{token}}

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
