info:
  name: Register
  type: http
  seq: 3

http:
  method: POST
  url: "{{backend-base-url}}{{api-stage}}/register"
  body:
    type: json
    data: |-
      {
        "email": "teszt@furnariusrufus.com",
        "password": "Pass1234"
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
