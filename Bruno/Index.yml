info:
  name: Index
  type: http
  seq: 2

http:
  method: GET
  url: "{{backend-base-url}}{{api-stage}}/"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
