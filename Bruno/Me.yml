info:
  name: Me
  type: http
  seq: 3

http:
  method: GET
  url: "{{backend-base-url}}{{api-stage}}/auth/me"
  headers:
    - name: Authorization
      value: Bearer {{token}}

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
