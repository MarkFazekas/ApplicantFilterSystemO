info:
  name: Login
  type: http
  seq: 2

http:
  method: POST
  url: "{{backend-base-url}}{{api-stage}}/auth/login"
  body:
    type: json
    data: |-
      {
        "email": "teszt@furnariusrufus.com",
        "password": "Pass1234"
      }
  auth: inherit

runtime:
  scripts:
    - type: after-response
      code: |-
        var data = res.getBody(); 
        if ("access_token" in data){
          bru.setEnvVar("token", data.access_token);
        }

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
